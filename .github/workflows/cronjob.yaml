name: Rebuild site (cron)

on:
  schedule:
    - cron: "25 19 * * *"

jobs:
  trigger-webhook:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Webhook
        run: |
          curl -X GET '${{ secrets.VERCEL_DEPLOY_HOOK }}'
